-- liquibase formatted sql

-- changeset mr486:1764772290881-1
CREATE TABLE amis (
id BIGINT GENERATED BY DEFAULT AS IDENTITY NOT NULL,
nom VARCHAR(255) NOT NULL,
avatar_url VARCHAR(255),
is_guest BOOLEAN,
CONSTRAINT pk_amis PRIMARY KEY (id));

